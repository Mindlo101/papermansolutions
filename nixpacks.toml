web: uvicorn app.main:app --host[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.deploy]
cmds = ["uvicorn app.main:app --host 0.0.0.0 --port $PORT"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT" 0.0.0.0 --port $PORT